import { arianeeProtocolClient } from './arianee-protocol-client';

describe('arianeeProtocolClient', () => {
  it('should work', () => {
    expect(arianeeProtocolClient()).toEqual('arianee-protocol-client');
  });
});
