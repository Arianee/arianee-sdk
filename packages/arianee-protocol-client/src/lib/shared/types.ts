export type ProtocolDetails = {
  contractAdresses: {
    smartAssets: string;
    identity: string;
    aria: string;
    store: string;
    creditHistory: string;
    whitelist: string;
    event: string;
    message: string;
    userAction: string;
    updateSmartAssets: string;
  };
  httpProvider: string;
  gasStation: string;
  chainId: number;
};
